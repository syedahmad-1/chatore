I've thoroughly tested the `AuthController` endpoints by:

1.  **Creating `AuthControllerTest.java`**: A new unit test file specifically for the `AuthController`.
2.  **Mocking Dependencies**: Utilized `@MockBean` for `AuthService` and `JwtService` to isolate the controller logic during testing.
3.  **Correcting DTO Instantiations**: Adjusted test code to correctly instantiate `SignupRequest`, `AuthResponse`, `LoginRequest`, `UserProfileResponse`, `ChangePasswordRequest`, `RefreshTokenRequest`, `RefreshTokenResponse`, and `LogoutRequest` objects using their appropriate constructors or builder patterns.
4.  **Disabling Spring Security**: Added `@AutoConfigureMockMvc(addFilters = false)` to prevent Spring Security filters from interfering with the tests, allowing direct testing of controller responses.
5.  **Adding Missing Imports**: Ensured all necessary DTOs and services were correctly imported into the test class.
6.  **Refining Assertions**: Corrected JSON path assertions to accurately reflect the structure and content of the API responses.
7.  **Comprehensive Endpoint Coverage**: Implemented tests for all key endpoints: `/signup`, `/login`, `/me`, `/change-password`, `/refresh-token`, and `/logout`, ensuring they behave as expected with mock data.
8.  **Verifying Success**: Confirmed that all 7 test cases passed successfully.

This ensures the `Authontroller` functions correctly and robustly.