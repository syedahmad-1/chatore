version: '3.9'

services:

  postgres:
    image: postgres:16
    container_name: chatore-postgres

    environment:
      POSTGRES_DB: chatore
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres

    ports:
      - "5432:5432"